<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hezi">

    <application
        android:name=".activity.MApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidDemo">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LottieActivity" />
        <activity android:name=".activity.AddressPickerActivity" />
        <activity android:name=".activity.ConstraintLayoutActivity" />

        <activity android:name=".viewpager.ViewpagerActivity" />
        <activity android:name=".viewpager2.ViewPager2Activity" />
        <activity android:name=".viewpager2.horizontal.HorizontalActivity" />
        <activity android:name=".viewpager2.vertical.VerticalActivity" />
        <activity android:name=".viewpager2.radiogroup.RgActivity" />
        <activity android:name=".viewpager2.withTab.TabActivity" />
    </application>

</manifest>