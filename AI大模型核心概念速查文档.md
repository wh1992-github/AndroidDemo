# AI 大模型核心概念速查文档

## 1. 总览关系

在 AI 大模型语境里，可以先用一条链路理解：

```text
AI 人工智能
  -> Model 模型
    -> Large Language Model / LLM 大语言模型
      -> Application 应用
        -> Agent 智能体
          -> Harness 运行框架 / 执行外壳
            -> Tool 工具 / Memory 记忆 / RAG 检索 / Workflow 工作流
```

简单说：

- **AI** 是大领域。
- **Model** 是 AI 的核心能力载体。
- **LLM** 是一种擅长语言理解和生成的大模型。
- **Agent** 是能围绕目标自主规划、调用工具、执行任务的系统。
- **Harness** 是把模型、工具、上下文、安全策略、执行流程组织起来的运行框架。

## 2. 核心概念表

| 名词 | 中文 | 一句话解释 | 例子 |
|---|---|---|---|
| AI | 人工智能 | 让机器表现出理解、推理、生成、决策等智能行为的技术总称 | 图像识别、语音助手、自动驾驶、ChatGPT |
| Model | 模型 | 通过训练得到的函数或系统，输入数据后输出预测、文本、图片、动作等结果 | GPT、Claude、Gemini、Llama |
| Large Model | 大模型 | 参数规模大、训练数据多、能力泛化强的模型 | 大语言模型、多模态模型 |
| LLM | 大语言模型 | 专门处理文本语言的大模型，可以理解、生成、总结、翻译、写代码 | GPT-4、GPT-5、Llama、Qwen |
| Multimodal Model | 多模态模型 | 可以同时处理文本、图片、音频、视频等多种输入输出的模型 | 看图问答、语音对话、视频理解 |
| Agent | 智能体 | 不只是回答问题，而是能围绕目标规划步骤、调用工具、执行任务的系统 | 编程 Agent、浏览器 Agent、数据分析 Agent |
| Harness | 运行框架 / 执行外壳 | 把模型接入工具、上下文、记忆、安全限制、执行循环的工程框架 | Codex、LangChain Agent Runtime、自研 Agent 框架 |
| Tool | 工具 | Agent 可以调用的外部能力 | 搜索网页、运行代码、查数据库、读写文件 |
| Prompt | 提示词 | 给模型的输入指令，用来告诉模型任务、角色、约束和输出格式 | “请总结这篇文章” |
| Context | 上下文 | 模型当前能看到的信息，包括用户问题、历史对话、文件内容、工具结果 | 当前聊天记录、代码片段、搜索结果 |
| Token | 词元 | 模型处理文本的基本单位，不完全等于汉字或英文单词 | “人工智能”可能被切成多个 token |
| Inference | 推理 / 生成 | 模型已经训练好之后，根据输入生成输出的过程 | 输入问题，模型生成答案 |
| Training | 训练 | 用大量数据调整模型参数，让模型学会模式和能力 | 训练语言模型预测下一个词 |
| Fine-tuning | 微调 | 在已有模型基础上，用特定数据继续训练，让它更适合某类任务 | 用客服数据微调客服模型 |
| RAG | 检索增强生成 | 先从外部知识库检索资料，再让模型基于资料回答 | 企业知识库问答 |
| Embedding | 向量表示 | 把文本、图片等内容转成向量，用于相似度搜索 | 搜索“退款规则”找到相关文档 |
| Memory | 记忆 | Agent 保存和复用历史信息的机制 | 记住用户偏好、项目结构、长期任务状态 |
| Workflow | 工作流 | 固定或半固定的多步骤任务流程 | 需求分析 -> 写代码 -> 跑测试 -> 生成报告 |

## 3. AI 是什么

**AI = Artificial Intelligence，人工智能。**

它不是某一个模型，而是一个很大的技术领域。只要机器表现出类似人类的智能能力，都可以归到 AI 范围里。

常见 AI 能力：

- 识别：识别人脸、语音、图片内容。
- 理解：理解文本、代码、意图。
- 生成：生成文章、图片、代码、音乐。
- 推理：根据条件分析问题、推导结论。
- 决策：在多个选项中选择行动。
- 执行：调用工具完成任务。

AI 和大模型的关系：

```text
大模型是 AI 的一种实现方式，但 AI 不等于大模型。
```

传统 AI 也包括规则系统、机器学习、专家系统、搜索算法、推荐算法等。

## 4. Model 是什么

**Model = 模型。**

模型可以理解为一个经过训练得到的“能力函数”：

```text
输入 -> 模型 -> 输出
```

例子：

```text
输入：一张图片
模型：图像分类模型
输出：猫 / 狗 / 汽车
```

```text
输入：请解释 Kotlin 的 apply
模型：大语言模型
输出：apply 的解释和示例代码
```

模型本身通常包含：

- 参数：模型内部学习到的权重。
- 结构：比如 Transformer。
- 能力边界：模型擅长什么、不擅长什么。
- 上下文窗口：一次能处理多少输入内容。

## 5. 大模型是什么

**大模型 = 参数规模大、训练数据多、泛化能力强的模型。**

它的“大”主要体现在：

- 参数多。
- 训练数据规模大。
- 能处理的任务类型多。
- 具备一定泛化能力，不只会单一任务。

大模型分类：

| 类型 | 说明 | 例子 |
|---|---|---|
| 大语言模型 | 处理文本、代码、推理 | GPT、Llama、Qwen |
| 多模态大模型 | 同时处理文本、图片、音频、视频 | GPT-4o、Gemini |
| 图像生成模型 | 根据文本生成图片 | Stable Diffusion、DALL-E |
| 语音模型 | 语音识别、语音合成、实时语音对话 | Whisper、TTS 模型 |

## 6. LLM 是什么

**LLM = Large Language Model，大语言模型。**

它的核心能力是处理语言，包括自然语言和代码。

常见能力：

- 问答
- 总结
- 翻译
- 写代码
- 改代码
- 解释代码
- 信息抽取
- 文档生成
- 逻辑推理

LLM 的典型工作方式：

```text
用户输入 Prompt
  -> LLM 读取上下文
  -> 预测最合适的后续 token
  -> 生成回答
```

注意：

```text
LLM 本质上不是数据库。
它可能知道很多训练中学到的信息，但最新信息、私有信息、项目文件内容，需要通过上下文、工具或 RAG 提供。
```

## 7. Agent 是什么

**Agent = 智能体。**

普通模型通常是：

```text
你问一句 -> 模型答一句
```

Agent 更像是：

```text
接收目标
  -> 分析任务
  -> 制定步骤
  -> 调用工具
  -> 观察结果
  -> 调整计划
  -> 继续执行
  -> 给出最终结果
```

例子：

用户说：

```text
把 customview 模块的 Java 类迁移成 Kotlin，并保证逻辑不变。
```

普通 LLM 可能只给建议。

Agent 可以：

- 搜索 Java 文件。
- 阅读代码。
- 修改文件。
- 运行 Gradle 编译。
- 根据错误修复。
- 检查 git diff。
- 汇报风险。

Agent 通常由这些部分组成：

| 组成 | 作用 |
---|---|
| Model | 提供理解、推理、生成能力 |
| Prompt / System Instruction | 约束行为和角色 |
| Tool | 允许执行外部操作 |
| Memory | 保存历史状态和偏好 |
| Planner | 拆任务、排步骤 |
| Executor | 执行具体动作 |
| Evaluator | 检查结果是否满足目标 |
| Harness | 把上述部分组织起来运行 |

## 8. Harness 是什么

**Harness 可以理解为“模型运行框架”或“Agent 执行外壳”。**

模型本身只会根据输入生成输出，但真实应用需要很多工程能力：

- 怎么把用户问题传给模型。
- 怎么拼接系统提示词。
- 怎么截断或压缩上下文。
- 怎么让模型调用工具。
- 怎么限制危险操作。
- 怎么读取文件。
- 怎么运行命令。
- 怎么保存日志。
- 怎么处理工具返回结果。
- 怎么多轮循环执行。

这些都属于 Harness 的职责。

可以类比：

```text
Model 像发动机。
Harness 像整辆车的底盘、方向盘、刹车、仪表盘和控制系统。
Agent 像能自己开车去完成目标的司机系统。
```

更工程化地说：

| 层级 | 职责 |
---|---|
| Model | 生成文本、代码、决策建议 |
| Harness | 组织上下文、工具、安全策略、执行循环 |
| Agent | 基于 Harness 和 Model，对目标进行自主执行 |
| Application | 面向用户提供完整产品体验 |

## 9. Prompt 是什么

**Prompt = 提示词。**

它是给模型的输入指令。

简单 Prompt：

```text
解释一下 Kotlin 的 lateinit。
```

更完整的 Prompt：

```text
你是 Android 工程师。
请用 Java 对比 Kotlin 的方式解释 lateinit。
要求：
1. 给出适用场景
2. 给出错误用法
3. 给出 Android 示例
```

Prompt 常见组成：

| 部分 | 作用 |
---|---|
| Role | 指定角色，比如“你是资深 Android 工程师” |
| Task | 说明任务 |
| Context | 提供背景信息 |
| Constraint | 限制条件，比如“不要修改逻辑” |
| Format | 指定输出格式，比如 Markdown 表格 |

## 10. System Prompt 是什么

**System Prompt = 系统提示词。**

它比普通用户 Prompt 优先级更高，用来规定模型的身份、边界、安全规则和工作方式。

例子：

```text
你是一个代码助手。
修改代码前必须先阅读相关文件。
不能删除用户未要求删除的代码。
```

优先级通常可以理解为：

```text
系统指令 > 开发者指令 > 用户指令 > 普通上下文
```

## 11. Context 是什么

**Context = 上下文。**

模型每次回答时能看到的信息，就是上下文。

上下文可能包括：

- 当前用户问题。
- 历史对话。
- 系统提示词。
- 代码文件内容。
- 搜索结果。
- 工具运行输出。
- 图片内容。
- 数据库查询结果。

注意：

```text
模型不能自动知道你电脑上的所有文件。
必须把相关文件读进上下文，它才能基于这些内容分析。
```

## 12. Token 是什么

**Token = 模型处理文本的基本单位。**

它不完全等于“字”或“单词”。

例子：

```text
hello 可能是 1 个 token
artificial intelligence 可能是多个 token
人工智能 也可能被切成多个 token
```

Token 用途：

- 计算上下文长度。
- 计算模型输入输出成本。
- 控制最大输出长度。

常见说法：

```text
上下文窗口 128K token
```

意思是模型一次最多能处理约 128K token 的输入和输出总量。

## 13. Inference 是什么

**Inference = 推理 / 生成。**

它指模型训练好之后，实际使用模型生成结果的过程。

例子：

```text
输入：帮我写一个排序函数
模型推理
输出：排序函数代码
```

这里的“推理”不是只指数学推理，而是模型运行并生成答案的过程。

## 14. Training、Fine-tuning、Distillation

| 名词 | 中文 | 说明 |
---|---|---|
| Training | 训练 | 从大量数据中学习模型参数 |
| Pre-training | 预训练 | 用海量通用数据训练基础模型 |
| Fine-tuning | 微调 | 在基础模型上用特定数据继续训练 |
| Instruction Tuning | 指令微调 | 让模型更会遵循人类指令 |
| RLHF | 人类反馈强化学习 | 用人类偏好优化模型回答 |
| Distillation | 蒸馏 | 用大模型能力训练小模型 |

区别：

```text
训练：从零或大规模学习能力。
微调：在已有能力上适配特定任务。
蒸馏：把大模型能力压缩给小模型。
```

## 15. RAG 是什么

**RAG = Retrieval-Augmented Generation，检索增强生成。**

它解决的问题是：

```text
模型不知道最新信息、公司内部资料、项目私有文档怎么办？
```

RAG 流程：

```text
用户问题
  -> 检索知识库
  -> 找到相关文档片段
  -> 把片段放进上下文
  -> 模型基于资料回答
```

例子：

用户问：

```text
我们公司报销规则是什么？
```

RAG 系统会先查公司文档，再让模型回答。

RAG 和微调区别：

| 对比 | RAG | 微调 |
---|---|---|
| 知识位置 | 外部知识库 | 模型参数里 |
| 更新成本 | 低，更新文档即可 | 高，需要重新训练 |
| 适合场景 | 知识问答、文档查询 | 风格、格式、专业任务习惯 |
| 可追溯性 | 强，可以引用来源 | 弱，不一定知道答案来自哪里 |

## 16. Embedding 是什么

**Embedding = 向量表示。**

它把文本、图片、音频等内容转换成一串数字向量。

向量的作用是表达语义相似度。

例子：

```text
“怎么申请退款”
“退款流程是什么”
```

这两句话字面不完全一样，但语义接近，Embedding 向量距离会比较近。

常见用途：

- 语义搜索。
- 推荐系统。
- RAG 文档检索。
- 聚类。
- 去重。

## 17. Tool Calling 是什么

**Tool Calling = 工具调用。**

模型自己不能真正访问网页、运行代码、查数据库，必须通过工具。

例子：

```text
用户：查一下今天北京天气。
模型：决定调用 weather 工具。
工具：返回天气数据。
模型：把结果整理成自然语言。
```

工具调用让模型从“只会说”变成“能做事”。

常见工具：

| 工具 | 能力 |
---|---|
| Web Search | 搜索互联网 |
| Shell | 运行命令 |
| File System | 读写文件 |
| Database | 查询数据库 |
| Browser | 操作网页 |
| Code Interpreter | 执行代码和分析数据 |

## 18. Function Calling 是什么

**Function Calling = 函数调用。**

它是工具调用的一种常见形式。

开发者定义函数：

```json
{
  "name": "get_weather",
  "parameters": {
    "city": "string"
  }
}
```

模型判断需要查天气时，会生成结构化调用：

```json
{
  "city": "北京"
}
```

然后程序真正执行函数，再把结果交回模型。

## 19. Memory 是什么

**Memory = 记忆。**

它让 Agent 能跨轮次保存信息。

记忆类型：

| 类型 | 说明 | 例子 |
---|---|---|
| Short-term Memory | 短期记忆 | 当前对话上下文 |
| Long-term Memory | 长期记忆 | 用户偏好、项目习惯 |
| Episodic Memory | 事件记忆 | 上次做过什么任务 |
| Semantic Memory | 语义记忆 | 稳定知识，比如项目结构 |

注意：

```text
记忆不是模型参数本身。
它通常是外部存储，再在需要时加载进上下文。
```

## 20. Workflow 是什么

**Workflow = 工作流。**

它是固定或半固定的任务步骤。

例子：

```text
代码修改工作流：
1. 阅读需求
2. 搜索相关代码
3. 修改文件
4. 运行测试
5. 检查 diff
6. 总结结果
```

Workflow 和 Agent 的区别：

| 对比 | Workflow | Agent |
---|---|---|
| 步骤 | 更固定 | 更自主 |
| 灵活性 | 较低 | 较高 |
| 可控性 | 强 | 需要更多约束 |
| 适合 | 稳定重复流程 | 开放复杂任务 |

## 21. Model、Agent、Harness 的区别

| 对比项 | Model | Agent | Harness |
---|---|---|---|
| 中文 | 模型 | 智能体 | 运行框架 / 执行外壳 |
| 核心能力 | 生成和理解 | 规划和执行 | 组织和控制执行环境 |
| 是否能调用工具 | 本身不能，需外部系统支持 | 通常可以 | 负责接入工具 |
| 是否有目标循环 | 没有 | 有 | 提供循环机制 |
| 类比 | 大脑能力 | 会做事的助手 | 助手工作的系统环境 |

一句话区分：

```text
Model 负责想和说。
Agent 负责为了目标持续做事。
Harness 负责让 Agent 能安全、有序、可控地运行。
```

## 22. Chatbot 和 Agent 的区别

| 对比 | Chatbot | Agent |
---|---|---|
| 主要行为 | 对话回答 | 执行任务 |
| 主动性 | 低 | 高 |
| 工具使用 | 可有可无 | 通常需要 |
| 任务跨度 | 单轮或短多轮 | 可持续多步骤 |
| 例子 | FAQ 客服机器人 | 自动修复代码并跑测试的编程助手 |

不是所有聊天机器人都是 Agent。

判断标准：

```text
能不能围绕目标多步执行，并根据结果调整下一步。
```

## 23. Copilot 和 Agent 的区别

| 对比 | Copilot | Agent |
---|---|---|
| 定位 | 辅助人完成任务 | 更主动地完成任务 |
| 控制权 | 人主导 | Agent 可部分主导 |
| 行为 | 提建议、补全、解释 | 规划、调用工具、修改、验证 |
| 例子 | 代码补全助手 | 自动迁移模块并修复编译错误 |

## 24. AI 应用的常见架构

一个典型 AI 应用可能长这样：

```text
用户
  -> 前端页面
  -> 后端服务
  -> Prompt 构造
  -> RAG 检索
  -> Model 推理
  -> Tool Calling
  -> 结果校验
  -> 返回用户
```

如果是 Agent 应用：

```text
用户目标
  -> Agent Harness
  -> Planner 规划
  -> Model 判断
  -> Tool 执行
  -> Observation 观察
  -> 再规划
  -> 最终结果
```

## 25. 常见容易混淆的词

| 名词 | 容易误解 | 正确理解 |
---|---|---|
| AI | 等于 ChatGPT | ChatGPT 只是 AI 应用之一 |
| Model | 等于应用 | 模型只是核心能力，不等于完整产品 |
| LLM | 什么都知道 | LLM 不是数据库，可能需要检索和工具 |
| Agent | 更大的模型 | Agent 是系统形态，不一定模型更大 |
| Harness | 模型本身 | Harness 是模型外层的工程运行框架 |
| Prompt | 魔法咒语 | Prompt 是任务说明和约束输入 |
| Context | 永久记忆 | Context 只是当前可见信息 |
| Memory | 模型参数 | Memory 通常是外部存储 |
| RAG | 微调 | RAG 是检索资料，不是训练模型 |
| Tool Calling | 模型真的会操作系统 | 实际操作由外部程序完成，模型只决定调用 |

## 26. 一句话记忆

| 名词 | 记忆方式 |
---|---|
| AI | 让机器表现出智能行为的大领域 |
| Model | 训练出来的能力核心 |
| 大模型 | 规模更大、能力更泛化的模型 |
| LLM | 擅长语言和代码的大模型 |
| Agent | 能围绕目标规划并执行的智能系统 |
| Harness | 把模型、工具、上下文和安全策略组织起来的运行框架 |
| Prompt | 给模型的任务说明 |
| Context | 模型当前能看到的信息 |
| Token | 模型处理文本的基本单位 |
| RAG | 先查资料，再让模型回答 |
| Embedding | 把内容变成可搜索的语义向量 |
| Tool | 让模型能做外部动作的能力接口 |
| Memory | 保存历史信息，后续再加载使用 |
| Workflow | 固定或半固定的任务流程 |

